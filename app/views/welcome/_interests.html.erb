<div class="bg-white rounded-lg shadow-lg">
  <div class="rounded-t-lg bg-cover bg-gradient-to-r from-red-500" style="background-image:
  linear-gradient(
  rgba(0, 0, 0, 0.45), 
  rgba(0, 0, 0, 0.45)
  ),
  url(<%= room.room_image.url(:large) %>);">

    <div class="bottom-left pt-24 p-2">
      <span class="text-1xl font-medium text-white"><%= room.title %></span>
    </div>
  </div>

  
  <div class="text-center p-2">
    <div id="subscribed_block_<%= room.id %>" class="subscribed_block_class">
    <%= render "subscriptions/subscription_index", room: room %>
    </div>
  </div>
</div>